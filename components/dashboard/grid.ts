/**
 * The dashboard table's column template, shared by the header row and every data row so
 * they cannot drift apart. Taken from the design mockup.
 *
 * RESPONSE DUE | TITLE | AGENCY | NOTICE TYPE | SET-ASIDE | NAICS | POSTED | TRIAGE
 */
export const TABLE_GRID = "126px minmax(200px,1fr) 156px 150px 78px 58px 56px 72px";

/** The table scrolls horizontally below this width rather than crushing the columns. */
export const TABLE_MIN_WIDTH = 940;
